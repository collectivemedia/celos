<?xml version="1.0"?>
<workflow-app xmlns="uri:oozie:workflow:0.4" name="celos test">
  <start to="foobar" />
  <action name="foobar">
    <java>
      <job-tracker>${jobTracker}</job-tracker>
      <name-node>${nameNode}</name-node>
      <main-class>com.collective.celos.ThisClassDoesNotExist</main-class>
    </java>
    <ok to="end" />
    <error to="kill" />
  </action>

  <kill name="kill">
    <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
  </kill>
  <end name="end" />
</workflow-app>
