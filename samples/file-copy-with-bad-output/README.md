This workflow is the same as [file-copy](../file-copy) but with an
output file that doesn't match what the workflow produces
(2013-12-20T1600.txt).

It is used to verify that the test runner chokes on such a workflow.
